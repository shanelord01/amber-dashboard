services:
  web:
    build: .
    container_name: amber-dashboard
    ports:
      - "5000:5000"
    env_file:
      - .env
    volumes:
      - amber-data:/app/amber.db
    restart: unless-stopped

volumes:
  amber-data:

