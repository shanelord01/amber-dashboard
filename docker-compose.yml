services:
  web:
    build: .
    container_name: amber-dashboard
    ports:
      - "5000:5000"
    env_file:
      - .env
    volumes:
      - ./amber.db:/app/amber.db
    restart: unless-stopped

