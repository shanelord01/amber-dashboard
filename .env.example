# .env.example
FLASK_SECRET=change-me
AMBER_BASE_URL=https://api.amber.com.au/v1
# Optional: if you know it already
SITE_ID=
# Price region fallback if prices endpoint needs it (state code like NSW, VIC, SA etc)
REGION=
# Scheduler pull frequency in minutes
PULL_MINUTES=60
